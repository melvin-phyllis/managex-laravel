# ============================================
# CONFIGURATION NAMECHEAP / HÉBERGEMENT MUTUALISÉ
# ============================================
# Copiez ce fichier vers .env sur votre hébergement
# 
# IMPORTANT: Sur hébergement mutualisé, les WebSockets
# ne sont PAS supportés. L'application utilise le polling
# AJAX automatiquement comme fallback.

APP_NAME=ManageX
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=Africa/Abidjan
APP_URL=https://votre-domaine.com

APP_LOCALE=fr
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=fr_FR

APP_MAINTENANCE_DRIVER=file

# Sécurité
BCRYPT_ROUNDS=12

# Logging - Niveau warning uniquement en production
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# ============================================
# BASE DE DONNÉES
# ============================================
# Namecheap fournit ces informations dans cPanel > MySQL
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=votre_username_dbname
DB_USERNAME=votre_username_user
DB_PASSWORD=votre_mot_de_passe

# ============================================
# SESSIONS - Fichiers (compatible mutualisé)
# ============================================
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true

# ============================================
# CACHE - Fichiers (compatible mutualisé)
# ============================================
# Note: Pour meilleures performances, activez Redis
# si disponible sur votre plan Namecheap
CACHE_STORE=file
CACHE_PREFIX=managex_

# ============================================
# QUEUE - Sync (exécution immédiate)
# ============================================
# Sur mutualisé, utilisez 'sync' car vous ne pouvez
# pas exécuter de workers en arrière-plan
# Alternative: 'database' avec CRON job
QUEUE_CONNECTION=sync

# ============================================
# BROADCAST - DÉSACTIVÉ pour mutualisé
# ============================================
# WebSockets non supportés sur hébergement mutualisé
# L'application utilise le polling AJAX automatiquement
BROADCAST_CONNECTION=log

# NE PAS configurer Reverb sur mutualisé
# REVERB_APP_ID=
# REVERB_APP_KEY=
# REVERB_APP_SECRET=
# REVERB_HOST=
# REVERB_PORT=
# REVERB_SCHEME=

# Désactiver le client WebSocket côté frontend
VITE_REVERB_APP_KEY=
VITE_REVERB_HOST=
VITE_REVERB_PORT=
VITE_REVERB_SCHEME=

# ============================================
# EMAIL - SMTP
# ============================================
MAIL_MAILER=smtp
MAIL_HOST=mail.votre-domaine.com
MAIL_PORT=465
MAIL_USERNAME=contact@votre-domaine.com
MAIL_PASSWORD=votre_mot_de_passe_email
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS="noreply@votre-domaine.com"
MAIL_FROM_NAME="${APP_NAME}"

# ============================================
# STOCKAGE FICHIERS
# ============================================
FILESYSTEM_DISK=local

# ============================================
# VITE
# ============================================
VITE_APP_NAME="${APP_NAME}"

# ============================================
# NOTES IMPORTANTES POUR NAMECHEAP
# ============================================
#
# 1. DÉPLOIEMENT:
#    - Uploadez les fichiers via FTP ou Git
#    - Le dossier public/ doit pointer vers public_html/
#    - Créez un lien symbolique: ln -s ../storage/app/public public/storage
#
# 2. BASE DE DONNÉES:
#    - Créez la BDD dans cPanel > MySQL Databases
#    - Importez le fichier SQL ou exécutez les migrations
#
# 3. PERMISSIONS:
#    - storage/ : 775
#    - bootstrap/cache/ : 775
#
# 4. CRON JOBS (optionnel, dans cPanel):
#    - Scheduler: * * * * * cd /home/username/public_html && php artisan schedule:run >> /dev/null 2>&1
#
# 5. FONCTIONNALITÉS AFFECTÉES SANS WEBSOCKETS:
#    - Les notifications temps réel utilisent le polling (toutes les 30s)
#    - La messagerie utilise le polling (toutes les 5s)
#    - Toutes les fonctionnalités restent opérationnelles
#
