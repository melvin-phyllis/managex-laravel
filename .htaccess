<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /managexV1/

    # Rediriger tout vers public/
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L,NC]
</IfModule>